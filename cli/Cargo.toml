[package]
name = "luadata_cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "luadata"
path = "src/main.rs"

[dependencies]
luadata = { path = ".." }
clap = { version = "4", features = ["derive"] }
